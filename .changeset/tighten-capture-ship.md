---
"@skydeo-aviato/saw": patch
---

Trimmed `/saw:capture` to its core intake flow (dropped the self-intake preamble and derived-work note, added an invocation trigger to its description) and told `/saw:ship` to fill out all required PR metadata.
