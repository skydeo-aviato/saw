{
  "name": "@skydeo-aviato/saw",
  "description": "Skydeo Agentic Workflow (SAW) — Claude Code skills that cut raw ideas into shipped code.",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=23.6.0"
  },
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && node scripts/sync.mjs version",
    "sync": "node scripts/sync.mjs",
    "release": "node scripts/tag-release.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.0"
  }
}
